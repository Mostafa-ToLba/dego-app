
  import 'package:flutter/material.dart';

class BookmarkScreen extends StatelessWidget {
     BookmarkScreen({Key? key}) : super(key: key);

    @override
    Widget build(BuildContext context) {
      return Center(child: Text('BookmarkScreen'));
    }
  }
